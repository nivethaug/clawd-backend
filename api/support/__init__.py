"""Live support system API package (user router + admin router).

Fully isolated feature: mounted additively in app.py, touches only the
support_* tables via services/support/. See router.py / admin_router.py.
"""
